# Database URL (SQLite for dev by default)
DATABASE_URL="file:./dev.db"

# JWT secrets (change in production)
JWT_ACCESS_SECRET="change_me_access_secret"
JWT_REFRESH_SECRET="change_me_refresh_secret"

# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
